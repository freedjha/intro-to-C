/* (The newline character ( \n ) is called an escape sequence, and it forces the cursor to 
change its position to the beginning of the next line on the screen.) */

#include <stdio.h>
int main()
{
    printf("hello world\nfrom PU");
return 0;
}
