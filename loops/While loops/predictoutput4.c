#include<stdio.h> 
int main() {
    int x = 5 ;
    printf("%d\n", x);      // x++ or x-- means use x and then increment 
    printf("%d\n", x++);   // ++x  or --x means increment x and then use
                        
    return 0;
}
